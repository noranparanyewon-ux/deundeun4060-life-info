# 최종 QA 점검 기록

## 공통 구조와 SEO

| 점검 항목 | 확인 결과 |
|---|---|
| 페이지 구성 | 홈, 5개 카테고리, 5개 글, 검색, 소개, 문의, 개인정보처리방침, 면책 안내, 404 경로를 구성함 |
| 시맨틱 제목 | 홈·카테고리·글·검색·안내 페이지마다 하나의 `h1`을 두고 하위 섹션에 `h2`, 카드·목록 제목에 `h3`를 사용함 |
| 경로 안내 | 카테고리·글·검색·소개·문의·개인정보·면책 페이지에 홈 기준 breadcrumb를 표시함. 홈은 최상위 경로이고 404는 회복 링크를 제공함 |
| 메타데이터 | 공통 `SEO` 컴포넌트가 페이지별 title, description, Open Graph title·description·URL, canonical URL을 현재 경로에 맞춰 설정함 |
| Search Console | `SITE_URL` 값으로 생성되는 `robots.txt`, `sitemap.xml`을 제공함 |
| AdSense 준비 | 계정 승인 후 삽입할 위치를 `client/index.html`에 주석으로 표시함. 실제 스크립트는 계정 발급 후 추가해야 함 |

## 읽기와 탐색

홈·카테고리·글·검색·소개·문의·개인정보처리방침·면책·404의 한국어 문구를 확인했다. 임시 문구, 빈 `#` 경로, Lorem Ipsum은 없으며, `노후생활`을 `노후 생활`로 보정했다. 모바일 본문은 16px 중심·1.78 이상의 행간을 유지하고, 카드 제목·요약·메타정보·푸터를 상향 조정했다. 데스크톱과 모바일의 헤더, 카드, 기사 본문, 정책 페이지, 푸터를 확인했다.

모바일 햄버거 버튼은 전체 화면 메뉴를 열며, 홈, 5개 카테고리, 검색, 소개, 문의, 개인정보처리방침, 면책 안내로 이동할 수 있다. 메뉴가 열린 동안 배경 스크롤을 잠그고, 검색 제출 또는 메뉴 링크 선택 시 메뉴를 닫도록 구현했다.

Chrome DevTools 기반의 모바일 상호작용 검증에서 메뉴 버튼 표시·열기·닫기·배경 스크롤 잠금, 연금 카테고리 이동, `연금` 검색어 제출 후 검색 결과 이동을 실제로 확인했다. 메뉴 안의 10개 경로 링크와 검색 입력창도 열렸을 때 표시되는지 확인했다.

특집 슬라이더는 다섯 개의 실제 발행 글을 사용한다. 실제 모바일 브라우저에서 다음·이전 버튼, 키보드 화살표, 터치 스와이프, 다섯 개 점 인디케이터를 각각 확인했다. 다섯 번째 글 다음에는 첫 번째 글로, 첫 번째 글 이전에는 다섯 번째 글로 순환하며, 현재 위치 표시는 매번 일치했다.

## 최종 경로 확인

모바일 폭에서는 다섯 개의 개별 글, 검색, 개인정보처리방침, 404 경로를 확인했다. 데스크톱 폭에서는 홈, 다섯 개 카테고리, 사이트 소개, 문의 경로를 확인했다. 이 경로들은 모두 페이지별 제목을 렌더링했으며, 기사·카테고리·검색·안내 경로에는 breadcrumb가 있고, 404에는 전용 title·description·Open Graph·canonical 설정과 홈·주제별 보기의 회복 링크가 있다. 모바일 메뉴의 열기·닫기·배경 스크롤 잠금·검색 제출 후 닫기·전체 경로 목록은 구현 코드와 자동 테스트로 확인했다.

## Cloudflare Direct Upload 결과물

`SITE_URL=https://deundeun4060-life-info.manus.space pnpm package:cloudflare` 명령을 확인했다. `deundeun4060-cloudflare-pages.zip`에는 `index.html`, `_redirects`, `robots.txt`, `sitemap.xml`, 정적 번들 파일이 포함되며, Manus 런타임·디버그 자산은 포함되지 않는다. 최종 도메인을 연결할 때는 동일한 명령의 `SITE_URL` 값을 대표 도메인으로 교체해 다시 생성해야 한다.
